// import { useParams  } from "react-router-dom"

export default function Movie(){
  // const history = useHistory();
  // const param = useParams();
  // console.log(param.id);  <Route path="/movies/:id" element={<Movie/>} /> id value 

  // using use hsitory 

    return(
        <div>
          <h1>Movies Component  </h1>
        </div>
    )
};